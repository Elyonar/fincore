<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.elyonar</groupId>
        <artifactId>core</artifactId>
        <version>0.0.1-SNAPSHOT</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <artifactId>customer</artifactId>
    <name>fincore :: core :: customer</name>
    <description>Customer module: profiles, KYC tier, lifecycle, mandates, and the customer-to-ledger-account
        mapping. The only module holding PII, which is what makes it the first extraction
        candidate (ADR 0006). Owns the `customer` schema; connects as `core_customer`. Never
        owns money.</description>

    <dependencies>
        <dependency>
            <groupId>org.elyonar</groupId>
            <artifactId>auth</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-jdbc</artifactId>
        </dependency>

        <!--
            OpenAPI annotations only, never the springdoc starter. This module contributes
            controllers to Core's document but serves no document of its own — the starter lives
            in services/core/app, which is the deployable. Version matches what springdoc 3.1.0
            resolves there; a second copy at a different version would put two incompatible
            annotation classes on one classpath.
        -->
        <dependency>
            <groupId>io.swagger.core.v3</groupId>
            <artifactId>swagger-annotations-jakarta</artifactId>
            <version>2.2.52</version>
        </dependency>
    </dependencies>
</project>
