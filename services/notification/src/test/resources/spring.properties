# Three datasources per context here — owner, app, worker — so the same bound applies for the same
# reason as Core's. See services/core/app/src/test/resources/spring.properties.
spring.test.context.cache.maxSize=2
